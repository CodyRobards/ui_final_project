/// Name of the environment variable that can override the planner storage
/// directory path.
const plannerStorageDirEnv = 'PLANNER_STORAGE_DIR';

/// File name used to persist planner data on disk.
const plannerStorageFileName = 'planner_data.json';
