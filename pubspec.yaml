name: planner_cli
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  args: ^2.5.0
  path_provider: ^2.1.4
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^4.0.0
  test: ^1.25.2

flutter:
  uses-material-design: true
